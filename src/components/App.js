import React from 'react';
import Game from '../containers/Game';
import '../assets/stylesheets/App.css';

const app = props => {
    return (
        <div className='App'>
            <Game />
        </div>
    );
};

export default app;
